> [!WARNING]
> **This page documents the previous deployment, not the live one.**
>
> It was generated by `forge doc` from `src/SLOW.sol` at commit
> [`a444cae`](https://github.com/z0r0z/slow/blob/a444cae9b3143d453da8c91a945ec2ad0e762432/src/SLOW.sol),
> where that file held what is now [`src/SLOWv1.sol`](../../../../src/SLOWv1.sol) — the
> 21,648-byte build. The contract live at
> `0x000000006513B7821171C8447ec7ECdfa3b956Fd` is the 24,466-byte build in
> [`src/SLOW.sol`](../../../../src/SLOW.sol), and it differs in ways that matter for
> security:
>
> | | this page (v1) | live build |
> | --- | --- | --- |
> | `commitGuardian` | permissionless | user or sitting guardian only |
> | `SLOWGate.claimMany` | atomic; one failure reverts the batch | per-id `try`, capped at 250,000 gas, failures skipped |
> | guardian approval counter | shared `nonces` | separate `guardianNonces` |
> | op types | `0` transfer/deposit, `1` withdraw | `0` transfer, `1` withdraw, `2` deposit |
> | dapp storage | `htmlChunk1` / `htmlChunk2` (SSTORE2) | immutable `page` pointer to `SlowPage` |
> | not present in v1 | — | `depositToWithPermit`, `depositToWithTipAndPermit`, `permitSelf`, `forgetInbound`, `predictDepositId`, the `SlowGuardianIndex` ward views |
>
> Everything below is accurate for v1 and is kept as the record of that build.
> **For the live contract, read [the README](../../README.md) and
> [`src/SLOW.sol`](../../../../src/SLOW.sol), which are authoritative.**
>
> These pages are not regenerated: the `forge doc` in this toolchain now emits a
> vocs site rather than this mdbook layout, so regenerating would swap doc engines
> and add an npm build to a repo that deliberately has none.

# SLOWGate
[Git Source](https://github.com/z0r0z/slow/blob/a444cae9b3143d453da8c91a945ec2ad0e762432/src/SLOW.sol)

`claim`-only operator for keeper-driven settlement. Approve via
`setApprovalForAll(slow.gate(), true)`. Cannot redirect funds: the gate has
no path to `safeTransferFrom` or `withdrawFrom`, and `claim` pins payout to `pt.to`.
Holds optional per-transfer relayer tips posted via `SLOW.depositToWithTip`.


## State Variables
### slow

```solidity
SLOW public immutable slow = SLOW(msg.sender)
```


### tips

```solidity
mapping(uint256 transferId => Tip) public tips
```


## Functions
### constructor


```solidity
constructor() payable;
```

### recordTip


```solidity
function recordTip(uint256 transferId, address sender, address to) public payable;
```

### claim

Settle one transfer through the gate. If a tip is attached, pays it
to `msg.sender` and routes through `slow.claimTipped` (no recipient approval
required); otherwise routes through `slow.claim`, which requires `pt.to` to
have approved the gate via `setApprovalForAll`.


```solidity
function claim(uint256 transferId) public;
```

### claimMany

Atomic batch settlement; the whole call reverts on the first failure.
Keepers must filter ids off-chain (timelock-expired, no guardian on `pt.to`).


```solidity
function claimMany(uint256[] calldata transferIds) public;
```

### refundTip

Recover an unclaimed tip after the underlying transfer cleared via a
non-gate path (`unlock`, `reverse`, `clawback`, or recipient-direct `claim`).
Callable only by the original depositor; reverts while the transfer is still pending.


```solidity
function refundTip(uint256 transferId) public;
```

### _claimAndPay


```solidity
function _claimAndPay(uint256 transferId) internal;
```

## Events
### TipPosted

```solidity
event TipPosted(
    uint256 indexed transferId, uint96 amount, address indexed sender, address indexed to
);
```

### TipRefunded

```solidity
event TipRefunded(uint256 indexed transferId, uint96 amount, address indexed to);
```

### TipPaid

```solidity
event TipPaid(uint256 indexed transferId, uint96 amount, address indexed to);
```

## Errors
### TipStillPending

```solidity
error TipStillPending();
```

### InvalidAmount

```solidity
error InvalidAmount();
```

### Unauthorized

```solidity
error Unauthorized();
```

### NoTip

```solidity
error NoTip();
```

## Structs
### Tip

```solidity
struct Tip {
    uint96 amount;
    address sender;
}
```

